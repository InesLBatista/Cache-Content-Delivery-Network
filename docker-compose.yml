version: '3.8'

services:
  # TODO:
  # 1. Define the 'origin' service:
  #    - Build from 'origin_server/' directory.
  #    - Mount 'origin_server/storage' as a volume.
  #    - Expose necessary ports.
  
  # 2. Define the 'cdn-node' service:
  #    - Build from 'cdn_node/' directory.
  #    - Mount 'cdn_node/cache' as a persistent volume.
  #    - Link to 'origin' and 'mqtt-broker'.
  
  # 3. Define the 'mqtt-broker' service:
  #    - Use an image like 'eclipse-mosquitto'.
  #    - Expose port 1883.
